# airline_operational_data
End-to-end airline data engineering project that models flight operations in a normalized OLTP database, implements an ELT pipeline, and builds a Star Schema data warehouse for analytics using role-playing airport dimensions.
